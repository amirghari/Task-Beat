package com.example.taskbeat.ui.viewmodels

import androidx.lifecycle.ViewModel
import com.example.taskbeat.data.DataRepository

class SettingsViewModel(private val dataRepo: DataRepository) : ViewModel() {
    // Define logic and observable states for settings
}
